name: Another
on:
  workflow_dispatch:
  workflow_run:
    workflows:
      - Changes
    types: [completed]

jobs:
  Context:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Debug
        env:
          WORKFLOW: ${{ toJson(github.event.workflow_run.event) }}
        run: |
          echo "$WORKFLOW"

